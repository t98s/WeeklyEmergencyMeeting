// use serenity::model::channel::Message;
// use serenity::model::id::ChannelId;
// use serenity::prelude::Context;
// use serenity::Result;

// pub async fn announce(ctx: &Context, cid: &ChannelId, mes: &str) -> Result<Message> {
//   cid.send_message(&ctx, |m| m.content(mes)).await
// }
